// id structure is an obligation for multi-account. PasswordList should be updated, the struct as well.

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>

char *strEncryption(int id, char *str)
{
    int length = strlen(str);
}

char charEncryption(int id, char ch)
{
}
